## 项目进展总结（截至 2026-08-20）

### 已完成

**1. 插件 `MuvluvUiTranslate` v0.1.1**（源码 `MuvluvUiTranslate\`，7 文件）
- Harmony 钩子：`TMP_Text.set_text` 单点（已验证无子类 override）+ `SetText` 9 重载 + `TextMeshPro`/`TextMeshProUGUI` 的 `OnEnable` Postfix（覆盖 prefab 烘焙文本）
- 词典：`exact` 精确匹配 + `patterns` 正则模板（数字归并），**F10 热重载**
- 捕获：未翻译日文去重计数落盘，含出现次数与组件名，跨运行累计
- 噪声治理（首轮实测后修复）：跳过 `TextViewText` 打字机组件（逐字前缀噪声 + 上游已译剧情通道）；`・`(U+30FB) 不再误判为日文
- 部署：分析副本与正式目录（`C:\Users\Benson\muv_luv_girlsgarden_cl`）双部署，GameAssembly MD5 一致零兼容风险

**2. 词典 v1：130 词条 + 12 正则模板**——游戏内实测生效（你已确认大量 UI 变中文）。覆盖：主界面导航、设置、关卡、战斗技能/增益、倒计时模板（`あと11日14時間→剩11天14小时`）、带 TMP 标签的标题等

**3. 数据管线（`tools\`）**
- `clean_capture.py`（python-ba 运行）：清洗捕获文件，本轮清掉 123 条噪声、留 177 条有效词
- `translate_pending.py`（**默认 python 环境**运行）：刚写完，参考 `PyTools/MyAwesomeTool/MyGptHelper.py` 的 `ask_deepseek_example` 直接调 DeepSeekChat。功能：读 pending → 过滤已译/超长(>80字剧情向)/无假名 → 30 条/批调 API（挂 30 组术语表、强制保留 TMP 标签和换行）→ 三次重试校验 → 合并写回词典（不动已有词条和 patterns）
  ```
  python tools/translate_pending.py --dry-run   # 先看会翻哪些
  python tools/translate_pending.py             # 正式翻译
  ```
  注意：写完还没实际跑过（API 调用路径未验证），首次跑建议先 `--dry-run`

### 运行循环（已跑通两轮）
玩游戏 → 捕获落盘 → `clean_capture.py` 清洗 → `translate_pending.py` 机翻入库 → 游戏 F10 热重载 → 游戏内校对

### 下一步（里程碑 2）
- 静态提取：跑 `Il2CppDumper-win-v6.7.46`（已解压未跑）出 DummyDll + stringliteral.json；UnityPy 扫 295 个 bundle 的 m_text——补齐纯汉字日文（运行时按设计不捕获）与未见过的界面
- 术语库从上游 `anosu/muvluvgg-translation` 仓库自动扩充
- 布局溢出个案处理（目前未收到反馈）